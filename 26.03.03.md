DataTable
db에서 가져온 데이터를 메모리에 담는 그릇

DataTable studentTable = new DataTable("Fruits"); //DataTable 생성
studentTable.Columns.Add("Name", typeof(string)); //열 추가
studentTable.Rows.Add("Kim",21,"Seoul"); //행 추가

DataTable dtChange=dtGrid.GetChange();
->변경된 행만 따로 가져오는 기능,수정 안하면 null 수정하면 그 행만 들어있음

ComboBox
Grid 컬럼이름=DataTable 컬럼이름=DB 컬럼 이름이 같아야 값이 들어감

ExcuteNonQuery()
결과를 받을 필요가 없는 insert,update문에 주로 사용한다
적용된 행의 개수를 리턴 해준다